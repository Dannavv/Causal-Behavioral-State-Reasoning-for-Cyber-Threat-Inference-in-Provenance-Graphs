# ZeroCausal Tuned Configuration
# Target: DARPA OpTC (Real Host Logs)

pcmci_alpha: 0.01
std_floor: 1.0

# Hybrid Anomaly Scorer (CAS) parameters (Tuned via Random Search)
a_p: 0.3632
b_p: 3.5284
a_r: 5.8843
b_r: 0.1495

# Conformal Calibrator parameters (Tuned)
target_fpr: 0.0822
conformal_lr: 0.0204
conformal_alpha_init: 0.05

# Adaptive Window (Change-point) Detector parameters
detector_short: 10
detector_long: 50
detector_threshold: 4.0
